# picblobs — position-independent code blob library
#
# Build all blobs:  bazel build //src/blob:all
# Run all tests:    bazel test //tests/...
# Lint C code:      bazel build --config=lint //src/...

exports_files([
    ".clang-tidy",
])
